#include<iostream>
#include<stack>
#include<vector>

using namespace std ;


class Solution {
public:
    void nextPermutation(vector<int>& nums) {
        /*
        3 steps
        1. find the pivot element where transition is happening
        2. now after the pivot till the last element reverse the arr
        3. now swap pivot idx, pivot idx+1
        */
    }
};



int main (){

    
    return 0;
}