#include<iostream>
#include<stack>
#include<vector>

using namespace std ;

class Solution {
public:
    void SubsetAdder(vector<vector<int>> subsets)
    vector<vector<int>> subsets(vector<int>& nums) {
        
    }
};


int main (){

    
    return 0;
}